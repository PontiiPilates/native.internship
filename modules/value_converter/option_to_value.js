/**
 * Скрипт для конвертации тегов option с сайта цк для здешнего селекта
 */

// let element = $('option');

// for (i=0; element.length > i; i++) {
//     text = element[i].innerHTML;
//     attr = element[i].setAttribute('value', text);
//     console.log(element[i]);
// }